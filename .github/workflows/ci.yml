name: Continous Integration
on:
  push:
    branches: [ main ]
  pull_request:
jobs:
  test:
    - name: Install app dependencies
      run: flutter pub get

    - name: Test app
      run: flutter test --coverage